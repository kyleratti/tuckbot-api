extends layout

block content
    h1 Mobility Mary's Magnificent Mirrors
    small THAT IS ILLEGAL!
    ul
        each vid in posts
            li #[a(href='/' + vid.redditPostId) #{vid.title}] 
                small also available on 
                    a(href='https://mirrorbot.ga/' + vid.redditPostId + '/') mirrorbot.ga
